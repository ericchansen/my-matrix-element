# Matrix environment variables
# Copy this to .env and fill in values before running docker compose.

# Your server's FQDN — used by Caddy, Synapse, and Element.
# For Azure: openclaw-XXXX.centralus.cloudapp.azure.com
# For custom domain: matrix.yourdomain.com
MATRIX_SERVER_NAME=changeme.centralus.cloudapp.azure.com

# PostgreSQL password — generate a strong random one
POSTGRES_PASSWORD=changeme
